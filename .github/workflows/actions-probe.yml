# TEMPORARY DIAGNOSTIC (2026-07-29). Every Actions run across the with-harmon org has failed with
# `startup_failure` / 0 jobs since 2026-07-28 23:41Z. This repo is PUBLIC, and on the Free plan public
# repositories get unlimited Actions minutes. If this workflow RUNS, the outage is metered-minutes/billing on
# the private repos. If it ALSO fails to start, the block is account-level and not about minutes at all.
# Delete once the question is answered.
name: actions probe
on: workflow_dispatch
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "actions can start"
